# ── Supabase (optional) ────────────────────────────────────────────────────
# Leave these EMPTY to run the portal on the fully-featured embedded demo
# dataset (Batches 29–38, 316 conflict-free classes, every page works).
# Fill them in to connect a real Supabase PostgreSQL backend
# (run supabase/schema.sql first — it creates tables, RLS and the conflict trigger).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
